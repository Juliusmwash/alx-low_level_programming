#include <stdio.h>
/**
 * main - prints the new name of the program
 * @argc: first argument
 * @argv: second argument
 *
 * Return: Always 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
