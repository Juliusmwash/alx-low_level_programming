/**
 * mod - gets the remainder of the two given numbers after division
 * @a: first number
 * @b: second number
 * Return: modulus of two given numbers
 */

int mod(int a, int b)
{
	return (a % b);
}
